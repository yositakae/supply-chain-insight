# Backend

Go backend service for Supply Chain Insight.

## Run

```powershell
cd backend
go run ./cmd/api
```

The server starts on `http://localhost:8080` by default.

## Endpoints

- `GET /health` returns the backend health status.
